get:
  summary: Listar clientes
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            $ref: '../schemas/client.yaml#/ClientList'

post:
  summary: Crear cliente
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../schemas/client.yaml#/CreateClient'
  responses:
    '201':
      description: Creado